// Site-wide constants you'll want to edit before launch.
// See README.md for the full checklist.

/** WhatsApp number in international format, digits only (no +, spaces or dashes). */
export const WHATSAPP_NUMBER = "212600000000";

/** Contact email shown in the footer. */
export const CONTACT_EMAIL = "hello@example.com";

export const BRAND_NAME = "[NAME]";

/**
 * Netlify Forms handles submission when this site is deployed on Netlify —
 * no backend required. If you deploy elsewhere (e.g. Vercel), swap the
 * <form> action in ContactSection.tsx for a service like Formspree,
 * or point it at your own endpoint. See README.md.
 */
export const CONTACT_FORM_NAME = "property-inquiry";
